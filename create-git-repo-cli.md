### Create-Repo-Cli.md
pm -S xdg-utils
Add this to your .zshrc so the system knows which browser to trigger:
  export BROWSER="/usr/bin/firefox" # Replace with your browser path 

gh auth login
  >GitHub.com
  >HTTPS        ssh is usually preferred for ArchDWM
  >Login with a web browser

gh auth status

